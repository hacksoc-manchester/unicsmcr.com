<!DOCTYPE html>
<html lang="en">

<head>
  <% include ../../partials/head.ejs %>
    <title>CV Bank | HackSoc</title>
    <link rel="stylesheet" type="text/css" href="/css/cvSubmission.css" />
</head>

<body>
  <div id="container">
    <% include ../../partials/navbar.ejs %>
      <div id="body">
        <section id="cv-bank-login">
          <h1>CV Bank</h1>
          <h3>Your Submission</h3>
          <br><br>
          <div class="container">
            <div class="row justify-content-center">
              <div class="col-md-6 align-left">
                <h5>Hello Kristijonas!</h5>
                <p>This is your submission on our CV bank</p>
                <hr>
                <div id="submission-container" class="container">
                    <div id="error-message" class="alert alert-danger"></div>
                    <div id="success-message" class="alert alert-success"></div>
                      <div class="row">
                        <p><b>Status:</b>&nbsp;
                          <div id="submission-status">
                            <%= locals.user.submissionStatus == true ? "published" : "private" %>
                          </div>
                        </p>
                      </div>
                      <div class="row">
                        <p><b>First name:</b>&nbsp;</p>
                        <div id="first-name" class="static-field"><%= locals.user.firstName %></div>
                        <div class="edit-field"><input type="text" value="<%= locals.user.firstName %>" name="firstName" placeholder="Enter your first name"
                            required></div>
                      </div>
                      <div class="row">
                        <p><b>Last name:</b>&nbsp;</p>
                        <div id="last-name" class="static-field"><%= locals.user.lastName %></div>
                        <div class="edit-field"><input type="text" value="<%= locals.user.lastName %>" name="lastName" placeholder="Enter your last name"
                            required></div>
                      </div>
                      <div class="row">
                        <p><b>Email:</b>&nbsp;</p>
                        <div><%= locals.user.email %></div>
                      </div>
                      <div class="row">
                        <p><b>Your CV:&nbsp;</b></p>
                        <div id="cv-link" class="cv-link static-field">
                          <a href="<%= locals.user.cvLink %>" target="_blank" rel="noopener noreferrer"><%= locals.user.cvLink %></a>
                        </div>
                        <div class="edit-field"><input type="text" value="<%= locals.user.cvLink %>"
                            name="cvLink" placeholder="Enter the link to your CV" required></div>
                      </div>
                </div>
                <div id="actions"class="row justify-content-center">
                  <input id="publish-button" class="submit-button smooth-transitions" type="button" value=" <%= locals.user.submissionStatus == true ? 'Make Private' : 'Publish' %>" onclick="publish()">
                  <input id="edit-button" class="edit-button smooth-transitions" type="submit" value="Edit" onclick="editSubmission()">
                </div>
                <div class="row justify-content-center">
                  <form action="/cv/logout" method="post">
                    <input id="logout-button" class="smooth-transitions" type="submit" value="Logout">
                  </form>
                </div>
              </div>
            </div>
          </div>
        </section>
      </div>
      <% include ../../partials/footer.ejs %>
  </div>
</body>
<script src="/js/formValidation.js"></script>
<script src="/js/cvSubmission.js"></script>

</html>